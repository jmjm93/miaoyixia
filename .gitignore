# Raw CC-CEDICT download (re-fetch with `npm run fetch-dict`)
vendor/

# Generated dictionary shards (rebuild with `npm run build-dict`)
extension/data/

# Test artefacts
test/screenshots/
icon-preview.png

# Store upload packages (rebuild with `npm run package`)
dist/

node_modules/
*.zip
*.crx
*.pem

# Batch API state (transient)
translations/.batch-state.json
translations/pilot-review.md
